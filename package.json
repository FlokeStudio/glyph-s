{
  "name": "glyph-s",
  "version": "2.8.0",
  "description": "Glyph Search 2.8 — universal offline-first search engine",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint lib test scripts",
    "benchmark": "node scripts/benchmark.mjs",
    "bundle:floke": "node scripts/bundle-floke.mjs",
    "bundle:obsidian": "node scripts/bundle-cjs.mjs",
    "vendor:sync": "node scripts/bundle-cjs.mjs",
    "build": "npm run bundle:obsidian && npm run bundle:floke"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "eslint": "^9.31.0",
    "globals": "^16.3.0",
    "vitest": "^3.2.4"
  }
}
